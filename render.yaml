services:
  - type: web
    name: protect-pdf-backend
    env: python
    buildCommand: |
      apt-get update && apt-get install -y qpdf
    startCommand: python app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
